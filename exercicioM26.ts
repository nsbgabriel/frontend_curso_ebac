function multiplicar(numero1: number, numero2: number): number {
    return numero1 * numero2;
}

const resultado = multiplicar(2, 3);
console.log(resultado);


function saudacao(nome: string): string {
    return `Olá ${nome}`;
}

const dizOla = saudacao("Gabriel");
console.log(dizOla);